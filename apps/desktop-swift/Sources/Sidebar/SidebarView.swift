import SwiftUI

struct SidebarView: View {
    @Bindable var viewModel: SidebarViewModel
    var body: some View {
        Text("Sidebar placeholder — replaced in Task 6")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
